.label.label-important
  %h1= "Mensajes enviados de #{get_usuario.username}"
%hr
- if @enviados.exists?
  %br/
  %table.table.table-bordered.table-no-lr-border
    - @enviados.each do |mensajepersonal|
      %tr
        %td
          %h3= link_to normalizar_texto(mensajepersonal.titulo,100), mensajepersonal
          Para
          %span.badge.badge-info= mensajepersonal.destinatario.username
        %td
          A las: #{mensajepersonal.created_at.to_s(:detallada)}
        %td= link_to 'Eliminar', mensajepersonal, confirm: 'Estás seguro?', method: :delete, class: "btn btn-danger btn-mini"
- else
  Aún no has enviado ningun mensaje.